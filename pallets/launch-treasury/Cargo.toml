[package]
name = "pallet-launch-treasury"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
description = "Validator-backed treasuries for launchpad tokens: fee slices staked as one pooled cooperator, yield returned as buy-and-burn (pallets/LAUNCH_TREASURY_SPEC.md)"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
log = { workspace = true }
parity-scale-codec = { workspace = true, default-features = false }
frame-support = { workspace = true, default-features = false }
frame-system = { workspace = true, default-features = false }
frame-benchmarking = { workspace = true, default-features = false, optional = true }
scale-info = { workspace = true, default-features = false, features = ["derive"] }
sp-core = { workspace = true, default-features = false }
sp-io = { workspace = true, default-features = false }
sp-std = { workspace = true, default-features = false }
sp-runtime = { workspace = true, default-features = false }
pallet-vitreus-dex = { workspace = true, default-features = false }
pallet-launchpad = { workspace = true, default-features = false }

[dev-dependencies]
proptest = "=1.5.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
pallet-balances = { workspace = true, features = ["std"] }
pallet-assets = { workspace = true, features = ["std"] }
primitive-types = { workspace = true, default-features = false, features = ["codec", "scale-info", "num-traits"] }

[features]
default = ["std"]
std = [
    "parity-scale-codec/std",
    "frame-benchmarking?/std",
    "frame-support/std",
    "frame-system/std",
    "log/std",
    "scale-info/std",
    "sp-core/std",
    "sp-io/std",
    "sp-std/std",
    "sp-runtime/std",
    "pallet-vitreus-dex/std",
    "pallet-launchpad/std",
]
runtime-benchmarks = [
    "frame-benchmarking/runtime-benchmarks",
    "frame-support/runtime-benchmarks",
    "frame-system/runtime-benchmarks",
    "sp-runtime/runtime-benchmarks",
    "pallet-vitreus-dex/runtime-benchmarks",
    "pallet-launchpad/runtime-benchmarks",
]
try-runtime = [
    "frame-support/try-runtime",
    "frame-system/try-runtime",
    "sp-runtime/try-runtime",
    "pallet-vitreus-dex/try-runtime",
    "pallet-launchpad/try-runtime",
]
